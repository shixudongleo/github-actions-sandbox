name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-20.04
    steps:
      - name: echo a string
        run: echo 'hello world!'
      - name: multiline scripts
        run: |
          node -v
          npm -v
            